/**
 * 이메일 주소마스킹
 * @param {string} email
 * @returns 
 */
export function maskEmail(email) {
    return email;
}

/**
 * 
 * @param {strinng} mobile 
 * @returns 
 */
export function maskMobile(mobile) {
    return mobile;
}