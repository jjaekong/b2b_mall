// import axios from "axios";

/**
 * @summary 사업자 실명인증
 * @param {string} bsnTitle 사업자명
 * @param {string} bsnNumber 사업자번호
 * @returns 
 */
export function certBusiness(bsnTitle, bsnNumber) {
    return true;
}